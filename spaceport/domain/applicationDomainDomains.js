define('domain/applicationDomainDomains', [], function() {
	// Private property of ApplicationDomain instances; each ApplicationDomain
	// has an associated Domain object which stores patches and other internal
	// information.
	return {}; // SpaceportID<ApplicationDomain> => Domain
});
