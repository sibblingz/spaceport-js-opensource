define('util/broadcast/listeners', [], function() {
	/*
	 * Any event that targets multiple objects at once is known as broadcast event.
	 * These events are managed through a dispatcher queue.
	 */
	return {};
});
